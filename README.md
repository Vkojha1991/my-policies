# my-policies
The application fetches policy data from a mocked API, filters active policies, sorts them by policy start date, and presents them as policy cards with pagination support. The UI adapts seamlessly across desktop and mobile devices while following accessibility best practices.
